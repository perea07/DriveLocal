/**
 * Generate a unique filename.
 * @param filename Filename.
 */
export declare function generateRandomFilename(filename: string): string;
/**
 * Remove properties from an object.
 * @param payload Payload.
 * @param keys List of properties to omit.
 */
export declare function omitProps<T, K extends keyof T>(payload: T, keys: K[]): Omit<T, K>;
