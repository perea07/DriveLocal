/**
 * Generate a unique filename.
 * @param filename Filename.
 */
export declare function generateRandomFilename(filename: string): string;
export declare type FunctionPredicate<T, K> = (entity: T, value: any, key: K) => Boolean;
/**
 * Omit properties.
 * @param obj Entity.
 * @param items Properties.
 */
export declare const omitProps: <T, K extends keyof T>(obj: T, items: K[]) => Partial<T>;
